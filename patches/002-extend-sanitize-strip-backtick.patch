--- a/graphiti_core/driver/falkordb_driver.py
+++ b/graphiti_core/driver/falkordb_driver.py
@@ -379,6 +379,11 @@
                 '|': ' ',
                 '/': ' ',
                 '\\': ' ',
+                # appmilla fork (TASK-FORK-PATCH bug #10): backtick survives
+                # upstream's strip list; markdown-style `path/to/file.md`
+                # references in episode bodies otherwise leak `` ` `` into
+                # entity names and break RediSearch syntax at index time.
+                '`': ' ',
             }
         )
         sanitized = query.translate(separator_map)
--- a/graphiti_core/driver/falkordb/operations/search_ops.py
+++ b/graphiti_core/driver/falkordb/operations/search_ops.py
@@ -83,6 +83,10 @@
         '|': ' ',
         '/': ' ',
         '\\': ' ',
+        # appmilla fork (TASK-FORK-PATCH bug #10): backtick survives
+        # upstream's strip list; see matching change in
+        # falkordb_driver.py:sanitize for rationale.
+        '`': ' ',
     }
 )
 
